contract contratoA {
    enum clienteTipo { socio , deudor , fiador , obligatorio }
    int saldoMinimo = 100000 ;
    function cobrar ( int saldoMaximo ) { //Revisar varios parametros
        int saldoTemporal = 11 ;
        int deudor = verificarCliente ( acutal , nombre ) ;

        while ( true == false ) {
            saldoTemporal = 10 ;
        }

        do {
            saldoTemporal = cambiarSaldoCorrespondiente ( ) ;
        }
        while ( false >= true ) ;

        for ( saldoTemporal = 0 ; true == true ; saldoTemporal  ) { //Revisar segunda expresion
            deudor = cambiarDeudor ( ) ;
        }

        if ( true == true ) { //Arreglar expresion booleana
            deudor = cambiarCuenta ( ) ;
        }

        if (false == false) {
            deudor = cuentaAnterior ( ) ;
        }
        else {
            deudor = cuentaActual ( ) ;
        }

        precioCobro = ( true == true ) ? false : true;

        return 20 ;
    }
}